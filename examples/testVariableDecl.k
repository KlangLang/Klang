// Declarações de viariaveis
integer x = 12;
integer y = x;

// Teste de atribuição
x = 1 + 2 * 4 / 7;

// Teste de chamada de função e argumentos
println(1);
print(1 + 2);
foo(x, y * 3);

// teste de escopo

{
}

{
    integer x = 1;
    x = x + 1;
    print(x);
}


// teste de declaração de função
public integer soma(integer a, integer b){
    return a + b;
}